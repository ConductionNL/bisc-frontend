mutation removeLanguageHouse($input: removeLanguageHouseInput!) {
    removeLanguageHouse(input: $input) {
        languageHouse {
            id
            name
            address
            email
            phoneNumber
            type
        }
        clientMutationId
    }
}
