mutation createSchedule($input: createScheduleInput!) {
    createSchedule(input: $input) {
        schedule {
            id
            resource
            repeatFrequency
            repeatCount
            freebusies {
                edges {
                    node {
                        id
                    }
                }
            }
        }
    }
}
