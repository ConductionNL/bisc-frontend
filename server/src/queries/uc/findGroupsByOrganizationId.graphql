query findGroupsByOrganizationId($organizationId: String!) {
    groups(organization: $organizationId) {
        edges {
            node {
                id
                name
                organization
            }
        }
    }
}
