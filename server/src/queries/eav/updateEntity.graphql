mutation updateEntity($input: updateEntityInput!) {
    updateEntity(input: $input) {
        entity {
            id
            type
            name
            description
            attributes {
                totalCount
                edges {
                    node {
                        id
                        name
                        format
                    }
                }
            }
        }
    }
}
