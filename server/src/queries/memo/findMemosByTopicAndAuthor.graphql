query findMemosByTopicAndAuthor($topic: String!, $author: String!) {
    memos(topic: $topic, author: $author) {
        edges {
            node {
                id
                name
                description
                author
                topic
            }
        }
    }
}
