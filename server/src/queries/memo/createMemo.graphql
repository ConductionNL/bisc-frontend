mutation createMemo($input: createMemoInput!) {
    createMemo(input: $input) {
        memo {
            id
            name
            description
            author
            topic
        }
    }
}
