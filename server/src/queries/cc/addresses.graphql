query addresses($id_list: [String!]) {
    addresses(id_list: $id_list) {
        edges {
            node {
                id
                houseNumber
                postalCode
            }
        }
    }
}
