query languageHouse($languageHouseId: String!) {
    taalhuis(taalhuisId: $taalhuisId) {
        id
        name
        address {
            street
            houseNumber
            houseNumberSuffix
            postalCode
            locality
        }
        email
        telephone
        type
    }
}
