query languageHouse($languageHouseId: String!) {
    languageHouse(languageHouseId: $languageHouseId) {
        id
        name
        address {
            street
            houseNumber
            houseNumberSuffix
            postalCode
            locality
        }
        email
        telephone
        type
    }
}
