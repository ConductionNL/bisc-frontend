query biscEmployee($biscEmployeeId: String!) {
    biscEmployee(biscEmployeeId: $biscEmployeeId) {
        id
        givenName
        additionalName
        familyName
        email
        telephone
        dateCreated
        dateModified
    }
}
