query aanbiederEmployee($userId: String!) {
    aanbiederEmployee(userId: $userId) {
        id
        givenName
        additionalName
        familyName
        email
        telephone
        dateCreated
        dateModified
        userRoles {
            id
            name
        }
    }
}
