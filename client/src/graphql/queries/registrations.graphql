query registrations($taalhuisId: String!) {
    registrations(taalhuisId: $taalhuisId) {
        id
        dateCreated
        status
        givenName
        additionalName
        familyName
        registrar {
            id
            organisationName
        }
    }
}
