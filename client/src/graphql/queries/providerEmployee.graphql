query providerEmployee($userId: String!) {
    providerEmployee(userId: $userId) {
        id
        givenName
        additionalName
        familyName
        email
        telephone
        dateCreated
        dateModified
        userRoles {
            id
            name
        }
    }
}
