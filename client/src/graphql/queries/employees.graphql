query employees($languageHouseId: String) {
    employees(languageHouseId: $languageHouseId) {
        edges {
            node {
                id
                givenName
                additionalName
                familyName
                email
                telephone
            }
        }
    }
}
