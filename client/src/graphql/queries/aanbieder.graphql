query aanbieder($id: String!) {
    aanbieder(id: $id) {
        id
        name
        address {
            # TODO: add id when available
            street
            houseNumber
            houseNumberSuffix
            postalCode
            locality
        }
        email
        telephone
        type
    }
}
