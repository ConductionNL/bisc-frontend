query provider($id: String!) {
    provider(id: $id) {
        id
        name
        address {
            # TODO: add id when available
            street
            houseNumber
            houseNumberSuffix
            postalCode
            locality
        }
        email
        telephone
        type
    }
}
