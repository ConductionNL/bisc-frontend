mutation updateLanguageHouse($input: updateLanguageHouseInput!) {
    updateLanguageHouse(input: $input) {
        languageHouse {
            id
            name
            address
            email
            phoneNumber
            type
        }
    }
}
