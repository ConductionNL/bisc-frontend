mutation updateProvider($input: updateProviderInput!) {
    updateProvider(input: $input) {
        provider {
            id
            name
            address
            email
            phoneNumber
            type
        }
        clientMutationId
    }
}
