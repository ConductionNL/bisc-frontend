mutation createProvider($input: createProviderInput!) {
    createProvider(input: $input) {
        clientMutationId
        provider {
            id
            name
            address
            email
            phoneNumber
            type
        }
    }
}
