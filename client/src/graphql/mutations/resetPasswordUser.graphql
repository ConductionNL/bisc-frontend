mutation resetPasswordUser($input: resetPasswordUserInput!) {
    resetPasswordUser(input: $input) {
        clientMutationId
        user {
            email
            id
            password
            token
            username
        }
    }
}
