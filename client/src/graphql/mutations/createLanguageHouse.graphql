mutation createLanguageHouse($input: createLanguageHouseInput!) {
    createLanguageHouse(input: $input) {
        languageHouse {
            id
            name
            address
            email
            phoneNumber
            type
        }
        clientMutationId
    }
}
