mutation createStudent($input: CreateStudentInputType!) {
    createStudent(input: $input) {
        id
        dateCreated
        status
        givenName
        additionalName
        familyName
        memo
        registrar {
            id
            organisationName
            givenName
            additionalName
            familyName
            email
            telephone
        }
    }
}
