apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "template.fullname" . }}-env
  labels:
  {{- include "template.labels" . | nindent 4 }}
data:
  env.js: |
    // This file is automatically generated

    window.ENVIRONMENT = {
      API_URL: '{{ .Values.config.backendUrl }}',
    }